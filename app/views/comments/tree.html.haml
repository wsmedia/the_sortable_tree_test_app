- content_for :css do
  = stylesheet_link_tag 'the_sortable_tree', :media => :screen
- content_for :js do
  = javascript_include_tag 'jquery.ui.nestedSortable'

%p= link_to raw('&larr; back'), '/'

= sortable_tree @comments, :new_url => new_comment_path, :max_levels => 3, :title => :name, :type => :comments_tree